{
  "version": 2,
  "installCommand": "cd ../..; pnpm install --frozen-lockfile",
  "buildCommand": "cd ../..; pnpm --filter @nexus/web build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
